Verified the full 211-fill-up history (2018-12-08 → 2026-04-17) is complete in gas_purchases.csv and that the seed/backfill loads all of it — the deployed app shows ~1 fill-up because the production DB is missing the rows, not the repo. Found and fixed the likely root cause of the past data loss: seed-owner.ts deleted all owner fill-ups before parsing the CSV, so any empty/failed parse would silently wipe the history. Reordered it to parse+validate first, abort (without deleting) on 0 rows, and log count/date-range. Lint clean, all 83 tests pass. Restoring the deployed data still requires running `npm run db:seed` (+ db:seed-events) against the production database, which needs DATABASE_URL credentials not available in this environment.